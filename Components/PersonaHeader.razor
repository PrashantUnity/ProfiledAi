@using ProfiledAi.Models
@using ProfiledAi.Utils
<div class="sidebar-header d-flex justify-content-between align-items-center">
    <div class="d-flex align-items-center">
        <div class="profile-icon" >J</div>
    </div>
    <div>
        <button class="icon-btn" data-bs-toggle="modal" data-bs-target="@Constant.GetProfileInputModel().GetCallerModelId()">
            <i class="bi bi-person-add"></i>
        </button>
        <button class="icon-btn" data-bs-toggle="modal" data-bs-target="@Constant.GetGeminiApiKeyInputModel().GetCallerModelId()">
            <i class="bi bi-gear"></i>
        </button>
    </div>
</div>